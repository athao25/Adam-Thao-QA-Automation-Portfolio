# Sauce Demo E2E Tests - Environment Configuration Template
# Copy this file to .env and customize for your environment
# Run: cp .env.example .env

# =============================================================================
# Application Settings
# =============================================================================

# Base URL for the application under test
BASE_URL=https://www.saucedemo.com

# =============================================================================
# Test Execution Settings
# =============================================================================

# Default timeout for test actions (in milliseconds)
DEFAULT_TIMEOUT=30000

# Timeout for expect assertions (in milliseconds)
EXPECT_TIMEOUT=10000

# Run tests in headless mode (true/false)
HEADLESS=true

# Number of retry attempts for failed tests (0 = no retries)
RETRIES=0

# Number of parallel workers (leave empty for auto-detection)
WORKERS=

# =============================================================================
# Browser Settings
# =============================================================================

# Slow down test execution by specified milliseconds (useful for debugging)
SLOW_MO=0

# Take screenshots on failure (always/never/only-on-failure)
SCREENSHOT=only-on-failure

# Record video (on/off/retain-on-failure/on-first-retry)
VIDEO=retain-on-failure

# Trace recording (on/off/retain-on-failure/on-first-retry)
TRACE=on-first-retry

# =============================================================================
# CI/CD Settings
# =============================================================================

# Set to 'true' when running in CI environment
CI=false

# Environment name (local/ci/staging/production)
NODE_ENV=local
